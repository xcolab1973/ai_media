name: HARM Trend Data Collector (Latest Single File)

on:
  schedule:
    # 日本時間 7:00, 11:00, 17:00, 22:00 に実行（UTCに変換）
    - cron: '0 22,2,8,13 * * *'
  workflow_dispatch:
    inputs:
      reason:
        description: '手動実行の理由'
        required: false
        default: '手動トリガーによる強制収集'

jobs:
  collect_and_push:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        # エラー回避のため cache: 'pip' の行を削除しました

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests

    - name: Run Trend Collector
      run: |
        python src/collector.py

    - name: Commit and Push latest trend
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        
        # trend フォルダの変更をステージング
        git add trend/
        
        git diff --staged --quiet || (git commit -m "chore: auto-update trend/latest.json ($(date +'%Y-%m-%d %H:%M:%S'))" && git push)
